def mainDir="strategy-test"
def frontDir = "frontend"
def backDir = "backend"

pipeline {
    agent any

    stages {
        stage('Pull Codes from Github'){
            steps{
                checkout scm
            }
        }
        stage('update npm packages') {
            steps {
                sh 'npm install'
                sh 'npm start'
            }
        }
        stage('update python packages') {
            steps {
                sh 'source backend/bin/activate'
                sh 'pip install -r requirements'
                sh 'python main.py'
            }
        }
    }
}
