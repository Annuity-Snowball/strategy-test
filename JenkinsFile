def mainDir="strategy-test"
def frontDir = "frontend"
def backDir = "backend"

pipeline {
    agent any

    stages {
        stage('Pull Codes from Github'){
            steps{
                checkout scm
            }
        }
        stage('update npm packages') {
            steps {
                sh 'echo npm install'
                sh 'echo npm start'
            }
        }
        stage('update python packages') {
            steps {
                sh 'echo source backend/bin/activate'
                sh 'echo pip install -r requirements'
                sh 'echo python main.py'
            }
        }
    }
}
